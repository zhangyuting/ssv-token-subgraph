{
  "name": "ssv-network-token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ssv-network-token",
    "create-local": "graph create --node http://localhost:8020/ ssv-network-token",
    "remove-local": "graph remove --node http://localhost:8020/ ssv-network-token",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ssv-network-token",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.81.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
